[project]
name = "hackropole"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pwn>=1.0",
    "pycryptodome>=3.21.0",
    "ruff>=0.8.5",
]
